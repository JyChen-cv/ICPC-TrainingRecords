#include <algorithm>
#include <cmath>
#include <cstdio>
#include <cstring>
#include <iostream>
#include <queue>
#include <vector>

const int N = 3e5;
int head[N + 2], edge[N + 2], tot, ver[N + 2], num[N + 2], v[N + 2], dis[N + 2];
int main() {
    int n, m;

    return 0;
}