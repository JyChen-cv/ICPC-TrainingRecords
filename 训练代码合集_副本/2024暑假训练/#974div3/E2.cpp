#include <algorithm>
#include <cmath>
#include <cstdio>
#include <cstring>
#include <iostream>

void solve() { int a; }
int main() {
  std::ios::sync_with_stdio(0);
  std::cin.tie(nullptr), std::cout.tie(nullptr);
  int times = 0;
  std::cin >> times;
  while (times--) {
    solve();
  }
  return 0;
}
