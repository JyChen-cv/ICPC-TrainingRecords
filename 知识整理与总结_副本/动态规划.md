# 动态规划



## 一些常见套路

1. 有乘法操作或者$\times （-1）$这种操作的时候，或者是满足物极必反的题目条件的时候要考虑维护极端满足和极端不满足两种情况进行转移

   eg：[Sorting By Multiplication](https://codeforces.com/contest/1861/problem/D) 

   eg:  具体题目记不清了，大概是类似于石子合并那样的区间dp，但是要求是用乘法来计算代价，需要同时维护极大和极小，因为负负得正。

2. 思考一个大问题没有头绪的时候往往从思考他的子问题开始

3. 有一类题目喜欢考察这种不对称的状态设计 eg: [Paint Charges](https://codeforces.com/contest/1927/problem/G) 

4. 

## 优化思路

### 总述：

动态规划算法可以看作是聪明的搜索，但是本质还是枚举搜索，所以大思路上就是优化搜索的那些方法

### 单调队列

条件：当外层循环变量控制后（对于内层循环可以看作常量的变量），内层循环有且仅有两个变量，其中一个变量还必须是和位置相关的，这时候我们可以按照这两种变量的关系来建立单调队列。

## 背包

### 01背包	

```c++
	
```

### 完全背包

```c++

```

### 多重背包，二进制分组优化

```c++

```

### 多重背包，单调队列优化

```c++

```

### 分组背包

```c++

```

区分题目：**Best Travel Plans** 

![image-20230706214005734](/Users/chenjiayou/Library/Application Support/typora-user-images/image-20230706214005734.png)

![image-20230706214055575](/Users/chenjiayou/Library/Application Support/typora-user-images/image-20230706214055575.png)

**误区** ：这道题目很容易想成分组背包，但是仔细观察发现待在原地的代价是一定的（所有情况下待在原地的代价都是1，这个不会改变），只是贡献在变化（随着待在原地的时间变长而降低），如果能想到这一点就不会把他当做一个复杂的优化背包来思考。

**具体思路** ：该问题可以使用优先队列（堆）来解决。首先，我们枚举最后停留的位置城市i，然后构建一个优先队列（最大堆）来维护前面i个城市可以停留一分钟可以体验的最多的旅行活动。我们将每个城市的初始活动数量放入优先队列中。接下来，我们按照从优先队列中取出最大值的顺序，每次取出一个城市，更新最大活动数量，并将该城市的活动数量减去减少值后再放回队列。同时，我们还需要维护剩余的旅行时间，每取出一个城市，剩余时间减一。直到优先队列为空或剩余时间为零时，我们停止取出城市并得到最大的旅行活动数量。

**具体步骤如下** ：

1. 首先，我们需要枚举最后停留的位置城市i，因为旅行的时间是有限的，最后一段时间需要用于移动到城市i。
2. 对于每个i，我们使用优先队列来维护前面i个城市可以停留一分钟可以体验的最多的旅行活动。
3. 我们从1号城市开始，依次将每个城市的初始活动数量放入优先队列中。
4. 计算剩余时间remainingTime，remainingTime = m - 城市1到i移动开销，并初始化当前活动数量curActivities为0。
5. 在剩余时间内，每次从优先队列中取出当前1分钟可以体验活动数量最多的城市，将其活动数量加到curActivities中。更新剩余时间，每次减1。将该城市的活动数量减去减少值，并将其放回优先队列中（如果减去减少值后仍然大于0）。
6. 当优先队列为空或剩余时间为0时，停止循环。
7. 更新最大活动数量maxActivities为当前活动数量curActivities和maxActivities的较大值。
8. 重复步骤2-9，直到枚举完所有的i。
9. 返回最大活动数量maxActivities。

**时间复杂度分析** 

1. 初始化优先队列：对于n个城市，需要将每个城市的初始活动数量放入队列，时间复杂度为O(nlogn)。
2. 枚举停留的位置城市i：需要对每个i进行枚举，时间复杂度为O(n)。
3. 每次取出城市和更新优先队列：在最坏情况下，每个城市最多会被取出m次，并且每次取出城市和更新队列的操作的时间复杂度为O(logn)。因此，这部分的时间复杂度为O(n * m * logn)。 综上所述，算法的时间复杂度为O(n * m * logn)。

空间复杂度分析 

1. 优先队列：需要使用一个优先队列来存储城市的活动数量和减少值，其空间复杂度为O(n)。
2. 其他辅助变量：只使用了常数个辅助变量，因此空间复杂度为O(1)。 综上所述，算法的空间复杂度为O(n)。



### 物品价格有损的背包

题目：[能量石](https://www.acwing.com/problem/content/736/)

题解：

```c++
#include <iostream>
#include <cstdio>
#include <algorithm>
#include <cmath>
#include <cstring>

const int N=10000;
struct group {
    int s;  //时间
    int e;  //能量
    int l;  //每一秒消耗d能量
} mapp[N+2];
int f[N+2],n;

bool cmp(group x,group y)
{
    return x.s*y.l<y.s*x.l;
}

int main()
{
    int t,tt=0;
    std::cin>>t;
    while (1) {
        tt++;
        if (tt>t) break;

        std::cin>>n;
        for (int i=1;i<=n;i++) {
            std::cin>>mapp[i].s>>mapp[i].e>>mapp[i].l;
        }
        std::sort(mapp+1,mapp+n+1,cmp);
        memset (f,0,sizeof(f));
        
        for (int i=1;i<=n;i++) {
            for (int j=10001;j>=mapp[i].s;j--) {
                f[j]=std::max(f[j],f[j-mapp[i].s]+std::max(0,mapp[i].e-mapp[i].l*(j-mapp[i].s)));
            }
        }
        int ans=0;
        for (int i=1;i<=10001;i++) ans=std::max(ans,f[i]);
        std::cout<<"Case #"<<tt<<": "<<ans<<std::endl;
    }

    return 0;
}

//有一个启发就是：面对这种时间啊之类的全局都会跟着改变的东西，怎样去合理维护呢？
//要么就是改变自己，来相对看出别人的改变，这样能改变的较少
//要么就用一个优秀的数据结构，满足这种范围性的改变
//要么就通过某种排序，确定自己的顺序就是最优秀的，这样就不存在顺序的问题了，只存在选还是不选的问题

//启发2:可以用调整法？来证明，例如这道题目
//i和i+1这两件物品
//若先选择i，则总能量为   e(i)+e(i+1)-s(i)*l(i+1);
//若先选择i+1，则总能量为 e(i+1)+e(i)-s(i+1)*l(i);
//那么现在对比，则可以进行排序
```

### 树上背包

1. 蓝书上面的标准题和模版转换
2. 一个和杨镇熙讨论的题目，二项式反演+树上背包（https://www.luogu.com.cn/problem/P6478）
3. 2025CCPC东北四省赛E题，两种背包过程都在提交记录里（https://codeforces.com/gym/105924/attachments）

## 最长子序列

### 最长上升子序列

```c++

```

### 最长公共子序列

```c++

```

### 最长公共上升子序列

```c++

```

## 状态压缩

![image-20240906202122888](/Users/chenjiayou/Library/Application Support/typora-user-images/image-20240906202122888.png)



## 复习

### 最低通行费 

数字三角形

```c++
// dp[i][j] 表示从左上角到达该位置的最小费用
dp[i][j]=std::min(dp[i-1][j],dp[i][j-1])+mapp[i][j];
```

### 方格取数

数字三角形

```c++
// 首先把这个问题看作是从左上角同时向右下角走两条路，其次这两条路的速度相同（这很重要）
// dp[i][j][k] 表示走了i步后，第一条路的横坐标是j，第二条路的横坐标是k
// 这样考虑的好处是，两个人不会走到对方已经走过的地方，因为两个人速度相同，所以两个人走到对方走过的地方只可能是相遇
dp[i][j][k]=std::max(dp[i][j][k],dp[i-1][j-1][k-1]);
dp[i][j][k]=std::max(dp[i][j][k],dp[i-1][j][k-1]);
dp[i][j][k]=std::max(dp[i][j][k],dp[i-1][j-1][k]);
dp[i][j][k]=std::max(dp[i][j][k],dp[i-1][j][k]);
if (i!=j) dp[i][j][k]+=mapp[j][i-j]+mapp[k][i-k];
else dp[i][j][k]+=mapp[j][i-j];
```

### 怪盗基德的滑翔翼

最子序列

```c++
// dp[i] 表示以第i号元素结尾的最长下降子序列
dp[i]=std::max(dp[j])+1; // j是i前面的且比j大的元素
// 可以通过使用二分查找在log时间内查到符合条件的最长序列的长度用来转移
// 最后的ans=std::max(dp[i]);
```

### 合唱队

区间DP+计数DP

```c++
// dp[i][j] 表示，站成i～j队形的可能方式
int deal1(int x,int y,int z) {
    return (mapp[x]<mapp[y]) + (mapp[x]<mapp[z]);
}
int deal2(int x,int y,int z) {
    return (mapp[x]<mapp[z]) + (mapp[y]<mapp[z]);
}
dp[i][j]+=dp[i+1][j]*deal1(i,i+1,j);
dp[i][j]+=dp[i][j-1]*deal2(i,j-1,j);

// 错误
// 由于状态设计不准确导致计数重复
```

```c++
// 正解
// dp[i][j][2] 表示站成i～j队形的方式，且最后一个人从左面/右面站立 （计数类状态设计一般不能带有“可能”字眼）
if (mapp[i]<mapp[i+1]) dp[i][j][0]+=dp[i+1][j][0];
if (mapp[i]<mapp[j]) dp[i][j][0]+=dp[i+1][j][1];
if (mapp[i]<mapp[j]) dp[i][j][1]+=dp[i][j-1][0];
if (mapp[j-1]<mapp[j]) dp[i][j][1]+=dp[i][j-1][1];
```

### 最大上升子序列和

最子序列

```c++
// dp[i] 表示以mapp[i]结尾的最大上升子序列和
dp[i]=std::max(dp[j])+mapp[i]; // j属于i前面的比i小的
// 可以用二分查找优化
    
ans = std::max(dp[i]);
```

### 拦截导弹

最子序列

### 最长公共子序列

最子序列

```c++
// dp[i][j] 表示第一个序列考虑到第i个，第二个序列考虑到第j个所形成的最长公共子序列
dp[i][j]=std::max(dp[i-1][j],dp[i][j-1]);
if (a[i]==b[j]) dp[i][j]=std::max(dp[i][j],dp[i-1][j-1]+1);
```

### 最长公共上升子序列

最子序列

```c++
// dp[i][j] 表示第一个序列考虑到第i个，第二个序列考虑到第j个且以a[i]所形成的最长公共上升子序列
if (a[i] == b[j]) {
    f[i][j] = std::max(f[i][j], 1);
    for (int k = 1; k < i; k++) {
    	if (a[k] < a[i]) f[i][j] = std::max(f[i][j], f[k][j - 1] + 1);
    }
} else
	f[i][j] = f[i][j - 1];
```

```c++
// 考虑优化可以压缩到n^2
// 观察数据，对于每一个j进行一个保存，记录上次查询的位置tag[j]和上次统计的最大值res[j]
int ans = 0;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            if (tag[j] == 0) tag[j] = 1;
            if (a[i] == b[j]) {
                f[i][j] = std::max(f[i][j], 1);
                for (int k = tag[j]; k < i; k++) {
                    if (a[k] < a[i]) res[j] = std::max(res[j], f[k][j - 1] + 1);
                }
                tag[j] = i;
                f[i][j] = std::max(f[i][j], res[j]);
            } else
                f[i][j] = f[i][j - 1];
        }
        // std::cout << f[i][n] << " ";
        ans = std::max(ans, f[i][n]);
    }

```

### 采药

背包模型

```c++
// dp[i][j] 表示考虑到第i个物品，所用空间为j的时候所能获得的最大价值
if (t[i]<=j) dp[i][j]=std::max(dp[i-1][j],dp[i-1][j-t[i]]+v[i]);
else dp[i][j]=dp[i-1][j];

// 通过逆序循环可以进行优化
// dp[j] 表示考虑到空间为j的时候所能获得的最价值
if (t[i]<=j) dp[j]=std::max(dp[j],dp[j-t[i]]+v[i]);
```

### 宠物小精灵之收服

背包模型+多维代价

```c++
// dp[i][j][k] 表示考虑到第i个物品，所用代价1为j，代价2为k时能获得的最大价值
if (t1[i]<=j && t2[i]<=k) dp[i][j][k]=std::max(dp[i-1][j][k],dp[i-1][j-t1[i]][k-t2[i]]+v[i]);
else dp[i][j][k]=dp[i-1][j][k];

// 通过逆序循环可以进行优化
// 两个代价纬度都是逆序循环
```

### 买书

背包模型+计数

```c++
dp[0] = 1;
for (int i = 1; i <= n; i++) {
    for (int j = 1; j <= 4; j++) {
        if (i >= mapp[j]) dp[i] += dp[i - mapp[j]];
    }
}
// 显然错误的，计数的背包一定不能把物品纬度循环放到里层，否则计数会区分先买后买的顺序，会导致计数次数变多

dp[0] = 1;
for (int i = 1; i <= 4; i++) {
    for (int j = 1; j <= n; j++) {
        if (j >= mapp[i]) dp[j] += dp[j - mapp[i]];
    }
}
```

### 潜水员

反转背包+多维度代价（价值）

原背包问题是，限定重量上线，问能拿到的最大价值

反转背包问题是，限定价值下限，问所需要的最小重量

```c++
memset (f,0x3f,sizeof(f));
f[0][0]=0;
for (int i=1;i<=k;i++) {
    for (int j=m;j>=0;j--) {
        for (int l=n;l>=0;l--) {
            int a=j-mapp[i].O;
            int b=l-mapp[i].N;
            if (a<0) a=0;   //小于0的情况是合法的（看作是所带资源大于需求）
            if (b<0) b=0;   //同理
            f[j][l]=std::min(f[j][l],f[a][b]+mapp[i].wei);
        }
    }
}
```

### 机器分配

分组依赖背包

```c++
// dp[i][j] 表示考虑到前i个公司，考虑到前j个机器所能得到的最大利益
for (int i=1;i<=n;i++) {
    for (int j=0;j<=m;j++) {
        for (int k=0;k<=j;k++) {
            dp[i][j]=std::max(dp[i][j],dp[i-1][j-k]+mapp[i][k]);
        }
    }
}
```

### Nazrin the Greeeeeedy Mouse

```c++
// dp[i][j][k] 表示前i次拿取，考虑前j个物品，体积为k的情况下所能拿到的最大价值
for (int i = 1; i <= m; i++) {
	for (int j = 1; j <= n; j++) {
		for (int k = 0; k <= sz[i - 1]; k++) {
			dp[i][j][0] = std::max(dp[i][j][0], dp[i - 1][j][k]); // 这里的k不是传统背包那样总量，是每次拿取都有一个新的sz，所以转移的时候要注意
		}
		for (int k = 1; k <= sz[i]; k++) {
			dp[i][j][k] = dp[i][j][k - 1]; // 这句感觉很多情况都会忘记，一定要注意
			if (k >= mapp[j].x) dp[i][j][k] = std::max(dp[i][j][k], dp[i][j - 1][k - mapp[j].x] + mapp[j].y); // 若拿
			dp[i][j][k] = std::max(dp[i][j][k], dp[i][j - 1][k]); // 若不拿
		}
	}
}
// 答案是dp[m][n][sz[m]]
// 感觉和机器分配那到题目有一些相似之处
// 感觉很多自己现场编和写的背包问题都不用刻意在意循环的顺序（指的是正反顺序），因为不涉及到压状态。
```

### 有依赖的背包问题

树形依赖背包

利用dfs顺序进行更新(树形DP？)

```c++
// dp[i][j] 表示考虑给i号节点分配j个空间所能产生的最大价值
// 自底向上更新

void dfs(int x, int fa) {
    for (int i = head[x]; i; i = nxt[i]) {
        int y = ver[i];
        if (y == fa) continue;
        dfs(y, x);
    }
    
    // 考虑如何把该节点的所有儿子合并，利用类似于分配机器那道题目的思路
    for (int i = head[x]; i; i = nxt[i]) {
        int y = ver[i];
        if (y == fa) continue;
        for (int j = m; j >= 0; j--) { // 避免自己更新自己
            for (int k = 0; k <= j; k++) {
                dp[x][j] = std::max(dp[x][j], dp[x][j - k] + dp[y][k]);
            }
        }
    }

    for (int i = m; i >= 0; i--) { // 倒叙更新->同上
        if (i < w[x]) // 强制保证购买此节点
            dp[x][i] = 0;
        else // 强制保证购买此节点
            dp[x][i] = dp[x][i - w[x]] + v[x];
    }
}
```

### 能量石

背包+流失代价

```c++
//有一个启发就是：面对这种时间啊之类的全局都会跟着改变的东西，怎样去合理维护呢？
//要么就是改变自己，来相对看出别人的改变，这样能改变的较少
//要么就用一个优秀的数据结构，满足这种范围性的改变
//要么就通过某种排序，确定自己的顺序就是最优秀的，这样就不存在顺序的问题了，只存在选还是不选的问题

//启发2:可以用调整法？来证明，例如这道题目
//i和i+1这两件物品
//若先选择i，则总能量为   e(i)+e(i+1)-s(i)*l(i+1);
//若先选择i+1，则总能量为 e(i+1)+e(i)-s(i+1)*l(i);
//那么现在对比，则可以进行排序
```

### 分组背包问题

```c++
// dp[i][j] 表示考虑前i组物品在j体积下的最大价值
for (int i=1;i<=n;i++) {
    for (int l=m;l>=0;l--) { // 注意这两个顺序和01和完全不同，这样保证了每次最多选一个
        for (int k=1;k<=s[i];k++) { // 这样能保证要选的话肯定选最好的一个
            if (l>=mapp[i][k].v) f[l]=std::max(f[l],f[l-mapp[i][k].v]+mapp[i][k].w);
        }
    }
}
```

### 金明的预算方案

分组背包解决依赖背包

### 多重背包三

单调队列优化转移

### 树的最长路径

不用换根DP，直接树形dp即可

```c++
// dp1表示当前节点所拥有的子树的最大直径
// dp2表示当前节点所拥有的子树的第二大直径
int dfs(int x,int fa) {
    int d1=0,d2=0;
    for (int i=head[x];i;i=nxt[i]) {
        int son=ver[i];
        if (son==fa) continue;
        int ls=dfs(son,x)+w[i];
        if (ls>=d1) {
            d2=d1;
            d1=ls;
        }
        else if (ls>d2) {
            d2=ls;
        }
        ans=std::max(ans,d1+d2);
    }
    return d1;
}
// 对于每个节点我们只需考虑子1+子2的最大值能否更新即可，子1+父亲的组合在根节点其实被考虑过了
```

### 树的中心

换根DP

```c++
int dfs(int x,int fa)   // 找到任意一个节点的所有孩子中的前两长距离
{
    
    for (int i=head[x];i;i=nxt[i]) {
        int son=ver[i];
        if (son==fa) continue;
        int ls=dfs(son,x)+w[i];
        if (ls>=d1[x]) {
            d2[x]=d1[x];
            d1[x]=ls;
            rem[x]=son;  // !
        }
        else if (ls>d2[x]) d2[x]=ls;
    }
    return d1[x];
}

// 找到任意一个节点的所有孩子和父亲链中的前两长的距离
int dfs2(int x,int fa,int down)
{
    if (down>=d1[x]) {
        d2[x]=d1[x];
        d1[x]=down;
        rem[x]=fa;
    }
    else if (down>d2[x]) {
        d2[x]=down;
    }

    for (int i=head[x];i;i=nxt[i]) {
        int son=ver[i];
        if (son==fa) continue;
        if (son==rem[x]) {
            dfs2(son,x,d2[x]+w[i]);
        }
        else {
            dfs2(son,x,d1[x]+w[i]);
        }
    }
}
```

### 二叉苹果树

```c++
// dp[i][j]表示给第i个节点分配了j条边所能产生的最大价值
// 基本同有依赖的背包那道题目的思路
// 但是这道题目对于所有当前节点的子节点的代价都是1，所以按理说可以简单很多？但是想不到怎么简化
```

### 战略游戏

树形DP

```c++
// 分析：任何一条边都至少有一端有士兵
// dp[i][2] 表示覆盖i子树所需要的最少士兵且该节点处是否有士兵
dp[x][1]=sum(std::min(dp[y][0],dp[y][1]))+1;
dp[x][0]=sum(dp[y][1]);
ans=std::min(dp[root][0],dp[root][1]);
// 当转移过于复杂的时候就该想想是不是状态定义的方向错了
```

### 皇宫看守

树形DP+状态机

```c++
// 分析：最近两个有士兵的节点的距离不超过3，且要保证叶子节点被覆盖
// dp[i][3] 表示覆盖i子树所需要的最少士兵且该节点距离最近的一个有士兵的节点的距离<=j
// dp[x][0] -> 当前节点有士兵
void dfs(int x) {
    dp[x][0] = w[x];
    for (int i = 0; i < (int)nod[x].size(); i++) {
        int son = nod[x][i];
        dfs(son);
        dp[x][2] += dp[son][1];
        dp[x][0] += std::min(std::min(dp[son][0], dp[son][1]), dp[son][2]);
    }

    int tag = 0, res = 1e9; // dp[x][1]需要保证子节点中至少有一个dp[son][0]被用到，所以单独拿出来转移
    for (int i = 0; i < (int)nod[x].size(); i++) {
        int son = nod[x][i];
        if (dp[son][0] <= dp[son][1]) {
            tag = 1;
            dp[x][1] += dp[son][0];
        } else {
            dp[x][1] += dp[son][1];
            res = std::min(res, dp[son][0] - dp[son][1]);
        }
    }
    if (!tag) dp[x][1] += res; // 叶子节点也被赋值，一定程度上避免了不合法方案
}
```

### 股票买卖V

状态机

```c++
// dp[i][3] 表示截止第i天且第i天状态为j所能获得的最大利润
// 0 -> 冷冻 1-> 有买入 2-> 无卖入且不为冷冻
dp[0][0] = dp[0][1] = -1e9;
for (int i = 1; i <= n; i++) {
    dp[i][0] = dp[i - 1][1] + mapp[i];
    dp[i][1] = std::max(dp[i - 1][1], dp[i - 1][2] - mapp[i]);
    dp[i][2] = std::max(dp[i - 1][0], dp[i - 1][2]);
}
// 想清楚各个状态之间是怎么转移的即可
```

### 设计密码

KMP状态机模型

```c++
// dp[i][50] 表示考虑到前i个字符串长度且以第i个字符结尾的字符串与目标串的匹配数量是j个

dp[i][j]=dp[i-1][j-1];
dp[i][0]=sum(dp[i-1][j])*25;
for (int i=0;i<m;i++) ans+=dp[n][i];
// 借助KMP算法的思路可以发现是错误的，忽略了失配也有贡献的转移
```

### Mondriaan's Dream(POJ2411)

状态压缩DP

```c++
// dp[i][j] 表示考虑到第i行且第i行的状态是j时的方案数
// 其中j是状态压缩维度，若j的第k位为1，则代表该位置上竖着放着一个长方形且向下的长方形，0表示其他状况
// 则转移要满足的条件要（设i-1行的状态为l）l&j==0 && (l｜j)任意两个0要相邻
dp[i]+=dp[i-1][k];
```

### 炮兵阵地

状态压缩DP

